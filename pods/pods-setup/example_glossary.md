# Example PODS Glossary (worked output)

A worked example of what `generate_glossary.py` produces for a fictional midstream operator, "Acme Midstream." Use it to sanity-check your own output. This is what gets installed at `/Workspace/.assistant/skills/acme-midstream-pods-glossary/SKILL.md`.

```markdown
---
name: acme-midstream-pods-glossary
description: |
  Customer-specific PODS schema mappings for Acme Midstream: physical
  table/column names -> canonical PODS concepts, the UNIT of every measure
  column, adopted modules, the route key, and business jargon. Use whenever a
  PODS / pipeline-integrity question touches Acme Midstream data. Compose with
  the pods-* enterprise skills.
metadata:
  version: "0.1.0"
parent: pods-overview
---

# Acme Midstream — PODS Glossary

Generated by `pods-setup` on 2026-06-03.

## Route key

Canonical `route_id` maps to physical `line_id`.

⚠️ Route key differs across tables:

| Table | Route-key column |
|---|---|
| `integrity.smartpig_features` | `line_ref` |
| `pipeline.hca_segments` | `line_id` |

## Measure column UNITS (critical)

Confirm the unit before comparing any two measure columns. Convert to meters with `pods_ft_to_m` before joins.

| Column | Unit |
|---|---|
| `integrity.smartpig_features.feature_md` | **ft** |
| `pipeline.centerline.measure` | **m** |
| `pipeline.hca_segments.begin_measure` | **m** |
| `pipeline.hca_segments.end_measure` | **m** |

## ILI (anomalies + runs)

- **Anomaly table**: `integrity.smartpig_features`
- **Anomaly columns**: feature_id=`feature_id`, run_id=`insp_id`, measure=`feature_md`, depth_pct=`depth_pct`, length_in=`length_in`, feature_type=`feat_type`
- **ERF stored?**: no — compute via B31G/RSTRENG UDFs
- **Runs table**: `integrity.ili_runs`
- **Run columns**: run_id=`insp_id`, run_date=`run_date`, vendor=`vendor`, tool_type=`tool`

## Pipe attributes (for integrity math)

| Attribute | Column |
|---|---|
| table | `engineering.pipe_segments` |
| od_in | `od_inches` |
| wt_in | `wall_thk_in` |
| smys_psi | `smys` |
| maop_psig | `maop` |

## Modules adopted

| Module | Status |
|---|---|
| IR | ✅ adopted |
| TVC | — not adopted |
| ILI | ✅ adopted |
| CP | ✅ adopted |
| SL | — not adopted |
| OFF | — not adopted |

## Line / segment business names

| Business term | route_id values |
|---|---|
| `Line 4` | `L04` |
| `the 30-inch` | `L04` |
| `river crossing` | `L04-RC` |

## Flow direction

`increasing_measure_is_downstream` — use this to interpret upstream/downstream.

## Tribal knowledge

- 2019 run was Baker UT; 2024 run was Rosen MFL — depth sizing not directly comparable
- Station equation at 1500+00 on L07 after the 2021 reroute

## How to use

1. Check units before comparing measures.
2. Use mappings; ask if missing.
3. Don't query non-adopted modules (TVC, SL, OFF here).
```
