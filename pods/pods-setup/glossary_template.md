# PODS Glossary Skill Template

The generated workspace skill follows this shape. `generate_glossary.py` substitutes values from the interview answers. The **Measure column UNITS** section is the most important — it is what stops the analytical skills from joining feet to meters silently.

```markdown
---
name: {{customer}}-pods-glossary
description: |
  Customer-specific PODS schema mappings for {{customer}}: physical
  table/column names -> canonical PODS concepts, the UNIT of every measure
  column, adopted modules, the route key, and business jargon. Use whenever a
  PODS / pipeline-integrity question touches {{customer}} data. Compose with
  the pods-* enterprise skills.
tags:
  - data-source:pods
  - tier:workspace
  - customer:{{customer}}
---

# {{customer}} — PODS Glossary

Generated by `pods-setup` on {{generated_at}}.

## Route key
Canonical `route_id` maps to physical `{{route_physical}}`.
(If inconsistent across tables, a per-table table is rendered here.)

## Measure column UNITS (critical)
| Column | Unit |
|---|---|
| `{{measure_col}}` | **{{ft_or_m}}** |

## ILI (anomalies + runs)
- Anomaly table, anomaly columns, ERF stored?, runs table, run columns

## Pipe attributes (for integrity math)
| Attribute | Column |
|---|---|
| od_in | `{{od_col}}` |

## Modules adopted
| Module | Status |
|---|---|
| IR / TVC / ILI / CP / SL / OFF | adopted / not adopted |

## Line / segment business names
| Business term | route_id values |
|---|---|
| `Line 4` | `L04` |

## Flow direction
increasing_measure_is_downstream (or otherwise)

## Tribal knowledge
- Free-text caveats

## How to use
1. Check units before comparing measures.
2. Use mappings; ask if missing.
3. Don't query non-adopted modules.
```
