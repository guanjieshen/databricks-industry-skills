# <Skill> schema reference

> Add a `## Contents` section here listing the `##` headings once this file
> exceeds ~100 lines, so a partial read still reveals the full scope.

## Tables

| Table | Grain | Key columns | Notes |
|---|---|---|---|
| `EXAMPLE_TABLE` | one row per … | `ID`, `SITEID` | … |

## Columns of note

- `COLUMN_A` — what it means, valid values, gotchas.

## Relationships

- `EXAMPLE_TABLE.PARENT_ID` → `PARENT.ID` (note composite keys, e.g. `+ SITEID`).
