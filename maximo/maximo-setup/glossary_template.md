# Glossary Skill Template

The generated workspace skill follows this shape. `generate_glossary.py` substitutes `{{...}}` placeholders from the interview answers.

```markdown
---
name: {{customer}}-maximo-glossary
description: |
  {{customer}}'s Maximo business glossary — maps {{customer}} terms (their site,
  region, and asset-class names) to Maximo schema (SITEIDs, CLASSSTRUCTUREIDs,
  status/worktype codes, custom columns) and records their industry and how they
  use Maximo. Use for ANY Maximo question that uses {{customer}} business
  vocabulary. Composes with the maximo-* skills.
metadata:
  version: "0.1.0"
parent: maximo-overview
---

# {{customer}} — Maximo Business Glossary

Generated by `maximo-setup` on {{generated_at}}. Edit when business terms or schema mappings change.

## Industry & Maximo usage

Captured in interview Batch 0 (confirms the profiler's `usage_profile`). Scopes which
skills apply — skip the O&G-only skills (`maximo-integrity`, `maximo-hse`) when the
industry isn't O&G / no `plusg*` tables.

- **Industry** → {{industry}}
- **Industry solutions** → {{industry_solutions}}
- **Modules used in Maximo** → {{modules_in_use}}
- **Handled outside Maximo** → {{modules_elsewhere}}
- **Maintenance maturity** → {{maintenance_maturity}}
- **KPIs tracked** → {{kpis}}

## Sites

Map business site names to `WORKORDER.SITEID` / `ASSET.SITEID` values.

| Business term | SITEID values |
|---|---|
{{#each sites}}
| `{{name}}` | {{ids}} |
{{/each}}

## Asset / location hierarchy

How customer hierarchy levels map to `LOCATIONS` parent chain.

{{#each location_hierarchy}}
- **{{name}}** → {{mapping}}
{{/each}}

## Asset classes

Map business asset names to `ASSET.CLASSSTRUCTUREID` values.

| Business term | CLASSSTRUCTUREID values |
|---|---|
{{#each asset_classes}}
| `{{name}}` | {{ids}} |
{{/each}}

## Criticality conventions

{{#each criticality}}
- **{{level}}** → {{rule}}
{{/each}}

## Status conventions

Customer's definition of "open" work orders:

```sql
WORKORDER.STATUS IN ({{open_statuses}})
```

## Work-type codes

| Business category | WORKTYPE values |
|---|---|
{{#each worktypes}}
| {{category}} | {{values}} |
{{/each}}

## Custom columns

Extensions beyond standard Maximo.

| Column | Meaning |
|---|---|
{{#each custom_columns}}
| `{{column}}` | {{description}} |
{{/each}}

## Custom tables

Customer-specific tables that join to standard Maximo.

| Table | Join + meaning |
|---|---|
{{#each custom_tables}}
| `{{table}}` | {{description}} |
{{/each}}

## Regulatory codes used

{{regulatory_codes}}

## Tribal knowledge

Things that have caused confusion in past data work.

{{#each tribal_knowledge}}
- {{this}}
{{/each}}

## How to use

When you encounter a business term used by the customer:
1. Check this glossary first.
2. If the term is here, use the mapping.
3. If not, ASK the user before guessing.
4. If you learn a new mapping during the session, suggest adding it to this file via a re-run of `maximo-setup`.
```
